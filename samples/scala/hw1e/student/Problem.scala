object Problem {
  /**
    * Checks whether a sequence of integer values is increasing
    * @aparam a the sequence (of length >= 1)
    * @return true if a(i) < a(i+1) for all adjacent elements
    */
   val isIncreasing = (a: Seq[Int]) => ...
}
